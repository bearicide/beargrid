window.BearGridDrop=(function(){function attach(zone,machine){const el=document.querySelector(zone);if(!el)return;['dragenter','dragover'].forEach(ev=>el.addEventListener(ev,e=>{e.preventDefault();el.classList.add('drag')}));['dragleave','drop'].forEach(ev=>el.addEventListener(ev,e=>{e.preventDefault();el.classList.remove('drag')}));el.addEventListener('drop',async e=>{const files=[...e.dataTransfer.files].filter(f=>f.type.includes('audio'));if(window.BearGridPads&&files.length)await BearGridPads.loadFiles(files,machine)});}return{attach}})();